services:

  cache:
    image: redis:7.0.4
    restart: always
    volumes:
      - ./data/cache:/data

  db:
    image: postgres:14.5
    restart: always
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=educaproject
      - POSTGRES_USER=educaadmin
      - POSTGRES_PASSWORD=43231323:Educa:2311345

  web:
    build: .
    command: ["./wait-for-it.sh", "db:5432", "--",
              "uwsgi", "--ini", "/code/config/uwsgi/uwsgi.ini"]
    restart: always
    volumes:
      - .:/code
    environment:
      - DJANGO_SETTINGS_MODULE=educaproject.settings.prod
      - POSTGRES_DB=educaproject
      - POSTGRES_USER=educaadmin
      - POSTGRES_PASSWORD=43231323:Educa:2311345
    depends_on:
      - db
      - cache

  daphne:
    build: .
    working_dir: /code/educaproject/
    command: ["../wait-for-it.sh", "db:5432", "--",
              "daphne", "-u", "/code/educaproject/daphne.sock",
              "educaproject.asgi:application"]
    restart: always
    volumes:
      - .:/code
    environment:
      - DJANGO_SETTINGS_MODULE=educaproject.settings.prod
      - POSTGRES_DB=educaproject
      - POSTGRES_USER=educaadmin
      - POSTGRES_PASSWORD=43231323:Educa:2311345
    depends_on:
      - db
      - cache

  nginx:
    image: nginx:1.23.1
    restart: always
    volumes:
      - ./config/nginx:/etc/nginx/templates
      - .:/code
    ports:
      - "80:80"
      - "443:443"